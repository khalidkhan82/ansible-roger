---
## Play 1 - this play configure Layer 3 interfaces on the CSR-Routers

- hosts: CSR_Routers
  become: yes

  tasks:
    - name: "Task 1 configure interfaces settings"
      ios_config:
        lines:
          - ip address {{ interfaces.GigabitEthernet1.ip }} {{ interfaces.GigabitEthernet1.mask }}
        parents: interface {{ interfaces.GigabitEthernet1.name }}
