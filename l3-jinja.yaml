---
- name: configure network devices
  hosts: CSR_Routers
  become: yes

  tasks:
    - name: configure l3 using Jinja 2 template
      cli_config:
        config: "{{ lookup('template', './templates/l3-config.j2') }}"